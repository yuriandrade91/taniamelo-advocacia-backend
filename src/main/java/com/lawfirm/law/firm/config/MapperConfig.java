package com.lawfirm.law.firm.config;

/**
 * Manual mapper registration removed to avoid duplicate ClientMapper beans.
 * MapStruct is configured to generate a Spring component for mappers
 * (componentModel = "spring"). Keep this placeholder file to avoid
 * accidental re-creation of a manual factory in the future.
 */
public final class MapperConfig {
    private MapperConfig() { /* utility class - no instances */ }
}
